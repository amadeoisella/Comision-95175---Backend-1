<html>
  <head>
    <!-- Handlebars inyecta la variable titulo acá -->
    <title>{{titulo}}</title>
    <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 0; background: #f4f4f9; }
    .container { padding: 20px; max-width: 800px; margin: 0 auto; }
    header { background: #333; color: white; padding: 15px; text-align: center; }
  </style>
  </head>
  <body>
    <!-- Esto trae el código del archivo header.handlebars -->
    {{> header}}

    <!-- ACÁ SE VA A METER EL CONTENIDO DE LA RUTA (home) -->
    {{{body}}}
  </body>
</html>